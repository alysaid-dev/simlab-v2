import { PetunjukReader } from "../components/PetunjukReader";

export default function PetunjukMahasiswa() {
  return (
    <PetunjukReader
      audience="MAHASISWA"
      pageTitle="Petunjuk Mahasiswa"
      breadcrumbLabel="Petunjuk Mahasiswa"
    />
  );
}
